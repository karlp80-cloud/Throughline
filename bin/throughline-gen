#!/usr/bin/env node
// Node shebang wrapper. The CLI's real entry point is at
// dist-cli/cli/src/index.js after `npm run build:cli`.
import('../dist-cli/cli/src/index.js').catch((err) => {
  // eslint-disable-next-line no-console
  console.error(err);
  process.exit(1);
});
