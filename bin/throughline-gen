#!/usr/bin/env node
// Node shebang wrapper. `npm run build:cli` produces
// dist-cli/throughline-gen.mjs as a single bundled ESM file.
import('../dist-cli/throughline-gen.mjs').catch((err) => {
  // eslint-disable-next-line no-console
  console.error(err);
  process.exit(1);
});
